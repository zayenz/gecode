language: cpp

git:
  depth: 20
  
branches:
  only:
    - master
    - develop
    - /^feature\/.*$/
    - /^release\/.*$/
    - /^hotfix\/.*$/
    
compiler:
  - clang
  - gcc
  
script:
  - ./configure
  - make
  - LD_LIBRARY_PATH=. make check
