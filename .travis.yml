language: cpp

git:
  depth: 20
  
branches:
  only:
    - master
    - develop
    - /^feature\/.*$/
    - /^release\/.*$/
    - /^hotfix\/.*$/
    
compiler:
  - clang
  - gcc
  
env:
  - LD_LIBRARY_PATH=.
  
script:
  - ./configure && make && make check
